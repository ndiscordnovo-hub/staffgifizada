export const metadata = {
  title: "Conversor de Formatos",
  description: "Converta entre PNG, JPG, WEBP, GIF, MP4, WEBM e MP3 direto no navegador.",
};

export default function ConvertLayout({ children }) {
  return children;
}
