export const metadata = {
  title: "Editor de Videos",
  description: "Corte, comprima, gire e converta videos MP4, WEBM e MOV no navegador.",
};

export default function VideoLayout({ children }) {
  return children;
}
