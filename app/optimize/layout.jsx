export const metadata = {
  title: "Otimizacao Inteligente",
  description: "Reduza o peso de imagens, GIFs e videos automaticamente para os limites do Discord.",
};

export default function OptimizeLayout({ children }) {
  return children;
}
