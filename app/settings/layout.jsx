export const metadata = {
  title: "Configuracoes",
  description: "Preferencias padrao de formato, qualidade e privacidade.",
};

export default function SettingsLayout({ children }) {
  return children;
}
