export const metadata = {
  title: "Editor de GIF",
  description: "Otimize, ajuste e converta GIFs animados direto no navegador. Suporte a lote.",
};

export default function GifLayout({ children }) {
  return children;
}
