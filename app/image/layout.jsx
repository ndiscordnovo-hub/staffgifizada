export const metadata = {
  title: "Editor de Imagens",
  description: "Corte, ajuste cores, redimensione e exporte imagens PNG, JPG e WEBP direto no navegador.",
};

export default function ImageLayout({ children }) {
  return children;
}
