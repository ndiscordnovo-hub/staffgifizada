export const metadata = {
  title: "Processamento em Lote",
  description: "Processe ate 15 arquivos de uma vez com a mesma configuracao.",
};

export default function BatchLayout({ children }) {
  return children;
}
