/node_modules
/.next
/out
/build
*.log
.DS_Store
.env*.local
.vercel

# Artefatos de deploy (não vão pro Git; a Vercel builda sozinha)
/deploy
*.zip

# Config local do Claude Code (pessoal)
.claude/settings.local.json
/deploy-square
gifedition-square.zip
